output:
  - success
  - failure

pre-commit:
  parallel: false
  commands:
    fmt:
      run: cargo fmt --all
      stage_fixed: true
    lint:
      run: cargo clippy --workspace --all-targets --all-features --locked -- -D warnings