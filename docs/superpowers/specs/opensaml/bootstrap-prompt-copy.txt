=== COPY FROM HERE (inclusive) ===

# Objetivo

Bootstrap de dos crates Rust en el workspace OpenAuth (openauth-rs):

1. opensaml — librería SAML 2.0 Service Provider (capa protocolo + bindings), equivalente a la parte alta de samlify (npm). La criptografía XML (XML-DSig, XML-Enc, C14N) va en bergshamra, no aquí.

2. samlify — crate delgada que SOLO re-exporta opensaml (API familiar tipo npm samlify, sin lógica propia).

openauth-saml y openauth-sso no se reemplazan en este PR; solo scaffolding + utilidades. Cero regresiones en tests existentes.

# Arquitectura

bergshamra          → XML-DSig, XML-Enc, C14N, XPath, KeysManager (pure Rust)
opensaml            → SP SAML: metadata, AuthnRequest, parse login/logout, bindings
samlify             → pub use opensaml::*; (alias de marca/API)
openauth-saml       → integración OpenAuth (fase posterior consume opensaml)
openauth-sso        → HTTP, sesión, replay (como Better Auth sobre samlify)

Referencias behavior (no commitear clones):

- Better Auth SSO: reference/upstream-src/1.6.9/repository/packages/sso/
- samlify (lectura / porting): reference/upstream-samlify/2.10.2/repository/ vía script

# Alcance M0 (este PR)

## Hacer

### 1. Crate opensaml

- Ruta: crates/opensaml/
- Registrar en workspace root Cargo.toml → members
- Añadir opensaml = { path = "crates/opensaml", version = "0.0.6" } en [workspace.dependencies]
- #![forbid(unsafe_code)]
- Sin dependencia de openauth-core (standalone)

Dependencias (workspace donde exista):

- base64, flate2, quick-xml, thiserror, url
- bergshamra = "0.4" solo con feature crypto-bergshamra (optional, default off en M0)

Features opensaml (Cargo.toml):

[features]
default = []
crypto-bergshamra = ["dep:bergshamra"]

Módulo opensaml::binding (mover/adaptar desde openauth-saml, copiar sin borrar origen aún):

- xml_escape(&str) -> String          — & " ' < > para XML
- html_escape(&str) -> String         — action/value en form POST
- deflate_raw_encode / deflate_raw_decode — wrapper flate2, raw deflate SAML Redirect
- base64_encode / base64_decode       — normalizar whitespace SAML
- saml_post_binding_form(...) -> String — HTML auto-submit
- redirect_binding_query(...) -> String — query sin firma; documentar SigAlg/Signature después

Esqueletos (stubs documentados):

- error — OpenSamlError
- sp — ServiceProvider
- idp — IdentityProvider
- metadata — generate_sp_metadata stub
- authn — create_login_request_redirect stub
- response — parse_login_response_post stub
- logout — stubs redirect/post
- crypto — trait XmlSecurityBackend; BergshamraBackend behind feature

lib.rs re-exports:

pub mod binding;
pub mod crypto;
pub mod error;
pub mod sp;
pub mod idp;
pub mod metadata;
pub mod authn;
pub mod response;
pub mod logout;

pub use error::OpenSamlError;
pub use sp::ServiceProvider;
pub use idp::IdentityProvider;

### 2. Crate samlify (solo re-export)

- Ruta: crates/samlify/
- Registrar en workspace members
- Dependencia única: opensaml = { workspace = true }
- src/lib.rs COMPLETO (único código):

//! Thin re-export crate. All SAML logic lives in opensaml.
#![forbid(unsafe_code)]
pub use opensaml::*;

- README: no es el npm samlify; es alias Rust de opensaml.

### 3. Referencia upstream samlify (gitignored)

- scripts/fetch-upstream-samlify.sh — tag v2.10.2
- reference/upstream-samlify/2.10.2/repository/
- reference/upstream-samlify/VERSION.md
- .gitignore como better-auth upstream
- No commitear clone

### 4. Documentación

- crates/opensaml/README.md — experimental, SP-only, tabla npm samlify vs opensaml vs bergshamra, roadmap
- crates/samlify/README.md — usar opensaml o samlify re-export

### 5. Tests M0 (solo opensaml)

- tests/binding.rs: deflate roundtrip, xml_escape golden, post form escapa <
- cargo fmt --all
- cargo clippy -p opensaml -p samlify --all-targets -- -D warnings
- cargo nextest run -p opensaml -p samlify

### 6. openauth-saml

Solo comentarios: // TODO(opensaml): delegate to opensaml::binding (M1 migration).
Sin cambiar tests openauth-saml / openauth-sso.

## No hacer en M0

- XMLDSig verify/sign completo
- Integrar openauth-sso
- Publicar crates.io
- Borrar openauth-saml
- IdP completo

# Estructura de archivos

crates/opensaml/
  Cargo.toml, README.md, src/lib.rs, src/error.rs
  src/binding/{mod,deflate,encoding,escape,post_form,redirect}.rs
  src/{sp,idp,metadata,authn,response,logout}.rs
  src/crypto/{mod,backend,bergshamra}.rs
  tests/binding.rs

crates/samlify/
  Cargo.toml, README.md, src/lib.rs  (solo pub use opensaml::*)

reference/upstream-samlify/VERSION.md
scripts/fetch-upstream-samlify.sh

# Workspace root — añadir members

"crates/opensaml",
"crates/samlify",

# Workspace dependencies

opensaml = { path = "crates/opensaml", version = "0.0.6" }
samlify = { path = "crates/samlify", version = "0.0.6" }

# crates/opensaml/Cargo.toml

[package]
name = "opensaml"
description = "SAML 2.0 Service Provider library (bergshamra for XML crypto)."
# version/edition/license/repository/homepage = workspace
[lib]
name = "opensaml"
[features]
default = []
crypto-bergshamra = ["dep:bergshamra"]
[dependencies]
base64.workspace = true
flate2.workspace = true
quick-xml.workspace = true
thiserror.workspace = true
url.workspace = true
bergshamra = { version = "0.4", optional = true }

# crates/samlify/Cargo.toml

[package]
name = "samlify"
description = "Re-exports opensaml (samlify-shaped crate name for Rust)."
[dependencies]
opensaml.workspace = true

# Script fetch-upstream-samlify.sh

Igual que fetch-upstream-better-auth.sh: shallow clone tngan/samlify tag v2.10.2 → reference/upstream-samlify/2.10.2/repository

# Roadmap (README only)

M1 parse_login_response_post + bergshamra verify
M2 Redirect binding query sig
M3 SLO
M4 EncryptedAssertion decrypt
M5 openauth-saml → opensaml + tests desde saml.test.ts

# Criterios de aceptación

- cargo nextest run -p opensaml -p samlify OK
- cargo clippy -p opensaml -p samlify --all-targets -- -D warnings OK
- samlify = solo pub use opensaml::*
- fetch-upstream-samlify.sh documentado
- sin regresión openauth-saml / openauth-sso
- AGENTS.md: no commitear upstream clone

# Convenciones

- Código/docs en inglés
- Copiar binding desde openauth-saml en M0
- bergshamra M1: trusted_keys_only + strict_verification en README

=== COPY UNTIL HERE (inclusive) ===
